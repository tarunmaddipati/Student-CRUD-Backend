CREATE TABLE `practise`.`student_practice` (
  `id` INT NOT NULL,
  `student_name` VARCHAR(45) NULL,
  `student_percentage` VARCHAR(45) NULL,
  `student_branch` VARCHAR(45) NULL,
  PRIMARY KEY (`id`));
